#include<stdio.h>
#include<stdlib.h>
int main()
  { int i;
    char a[20];
    printf("enter the string:");
    scanf("%s",a);
    printf("the entered string is: %s",a);
    for(i=0;a[i]!='\0';i++)
     { }
    printf("the length is %d",i);
    exit(0);
}
 
